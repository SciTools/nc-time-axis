# Updates the environment conda lock files.
# See the called workflow in the "scitools/workflows" repo for more details.

name: ci-locks

on:
  schedule:
    - cron: "3 0 * * 6"

  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions: {}

jobs:
  refresh_lockfiles:
    uses: scitools/workflows/.github/workflows/refresh-lockfiles.yml@28aea7a3a1eb58e1bbf9b1d9c8cb2de07892b67e
    secrets:
      AUTH_APP_ID: ${{ secrets.AUTH_APP_ID }}
      AUTH_APP_PRIVATE_KEY: ${{ secrets.AUTH_APP_PRIVATE_KEY }}

