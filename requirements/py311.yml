name: ncta-dev

channels:
  - conda-forge

dependencies:
  - python 3.11

# setup dependencies
  - setuptools >=61
  - setuptools-scm >=7

# core dependencies
  - cftime >=1.5
  - matplotlib-base >= 3.8.0
  - numpy >=1.25

# test dependencies
  - codecov
  - pytest >=6.0
  - pytest-cov

# dev dependencies
  - pip
  - pre-commit
