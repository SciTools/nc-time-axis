name: ncta-dev

channels:
  - conda-forge

dependencies:
  - python 3.9

# setup dependencies
  - setuptools >=61
  - setuptools-scm >=7

# core dependencies
  - cftime >=1.5
  - matplotlib
  - numpy >=1.21

# test dependencies
  - codecov
  - pytest >=6.0
  - pytest-cov

# dev dependencies
  - pip
  - pre-commit
