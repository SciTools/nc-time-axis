name: ncta-dev

channels:
  - conda-forge

dependencies:
  - python 3.7

# setup dependencies
  - setuptools
  - setuptools-scm

# core dependencies
  - cftime
  - matplotlib
  - numpy

# test dependencies
  - codecov
  - pytest>=6.0
  - pytest-cov

# dev dependencies
  - pip
  - pre-commit
