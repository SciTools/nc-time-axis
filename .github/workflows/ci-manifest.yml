# Checks the setuptools MANIFEST.in reconciles with the sdist.
# See the called workflow in the "scitools/workflows" repo for more details.

name: ci-manifest

on:
  pull_request:

  push:
    branches:
      - "main"
      - "v*x"
      - "!auto-update-lockfiles"
      - "!pre-commit-ci-update-config"
      - "!dependabot/*"
    tags:
      - "v*"

  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions: {}

jobs:
  manifest:
    name: "check-manifest"
    uses: scitools/workflows/.github/workflows/ci-manifest.yml@28aea7a3a1eb58e1bbf9b1d9c8cb2de07892b67e

